[phases.setup]
aptPkgs = ['python3', 'python3-pip']

[phases.install]
cmds = [
    'ln -s /usr/bin/python3 /usr/bin/python',
    'npm ci'
]

[phases.build]
cmds = ['npm run build']

[start]
# WHY IS THE SERVER RUNNING ON PORT 3000
cmd = 'PORT=3000 npm start & PORT=4000 node ./server/app.js'
